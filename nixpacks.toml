[phases.setup]
nixPkgs = ["ruby_3_3", "nodejs", "libxml2", "libxslt", "postgresql"]

[phases.install]
cmds = ["bundle install"]

[phases.build]
cmds = ["bundle exec rails assets:precompile"]

[start]
cmd = "bundle exec puma -C config/puma.rb"
