<div class="d-flex justify-content-between align-items-center mb-4">
  <h1><i class="bi bi-pencil"></i> Modifier commande <%= @order.number %></h1>
  <%= link_to '<i class="bi bi-arrow-left"></i> Retour'.html_safe, @order, class: "btn btn-outline-secondary" %>
</div>
<%= render 'form', order: @order %>
