Bonjour,

Veuillez trouver ci-joint notre commande N° <%= @order.number %> datée du <%= @order.order_date&.strftime('%d.%m.%Y') %>.

RÉSUMÉ DE LA COMMANDE :
<% @order.order_lines.includes(:product).each do |line| %>
- <%= line.product.name %> : <%= line.quantity %> x CHF <%= number_with_precision(line.unit_price, precision: 2) %> = CHF <%= number_with_precision(line.total, precision: 2) %>
<% end %>

TOTAL : CHF <%= number_with_precision(@order.total, precision: 2) %>

Merci de confirmer la réception de cette commande par retour d'email.

Cordialement,
ESHOP INDUNI
